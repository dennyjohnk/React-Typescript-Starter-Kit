import { Flex } from '@chakra-ui/react';
import React from 'react';

const SignInPageComp: React.FC = () => {
  return <Flex>Signin page</Flex>;
};

export const SignInPage = React.memo(SignInPageComp);
export default SignInPage;
